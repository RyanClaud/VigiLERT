specVersion: "v1"
serviceId: "vigilert"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vigilert-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
