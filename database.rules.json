{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null || root.child('users').child($userId).child('emergencyContacts').hasChild(auth.token.phone_number)",
        ".write": "auth != null && auth.uid === $userId"
      }
    },
    "emergency_contacts": {
      "$userId": {
        ".read": "auth != null || root.child('users').child($userId).child('emergencyContacts').hasChild(auth.token.phone_number)",
        ".write": "auth != null && auth.uid === $userId"
      }
    }
        },
        "profile": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "helmet": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "trips": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}